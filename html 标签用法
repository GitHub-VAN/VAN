<html>标签是 HTML 文档的根元素，它包含了整个网页的内容。它的开始标签 <html> 通常放在文档的最开始，它的结束标签 </html> 通常放在文档的最末尾。
<head> 标签是 HTML 文档的头部元素，它包含了一些关于文档的元信息，例如标题、字符集、样式表、脚本等。它的开始标签 <head> 和结束标签 </head> 必须放在 <html> 标签内部。
<title> 标签是 HTML 文档的标题元素，它定义了文档在浏览器标签栏或收藏夹中显示的标题。它的开始标签 <title> 和结束标签 </title> 必须放在 <head> 标签内部，并且只能有一个 <title> 标签。
<body> 标签是 HTML 文档的主体元素，它包含了文档的可见内容，例如文字、图片、链接、表格等。它的开始标签 <body> 和结束标签 </body> 必须放在 <html> 标签内部，并且只能有一个 <body> 标签。
<h1> 到 <h6> 标签是 HTML 文档的标题元素，它们定义了文档中不同级别的标题，从最大的 <h1> 到最小的 <h6>。它们的开始标签和结束标签必须成对出现，并且可以放在 <body> 标签内部。
<p> 标签是 HTML 文档的段落元素，它定义了文档中一个段落的内容。它的开始标签和结束标签必须成对出现，并且可以放在 <body> 标签内部。
<a> 标签是 HTML 文档的链接元素，它定义了文档中一个超链接，可以指向另一个网页或文档。它的开始标签和结束标签必须成对出现，并且可以放在 <body> 标签内部。它还需要有一个 href 属性，用来指定链接的目标地址。
<img> 标签是 HTML 文档的图像元素，它定义了文档中一个图片的显示。它只有一个开始标签，没有结束标签，并且可以放在 <body> 标签内部。它需要有一个 src 属性，用来指定图片的来源地址。它还可以有一些其他属性，例如 alt 用来指定图片的替代文字，width 和 height 用来指定图片的尺寸等。
<table> 标签是 HTML 文档的表格元素，它定义了文档中一个表格的结构。它的开始标签和结束标签必须成对出现，并且可以放在 <body> 标签内部。它可以包含一些子元素，例如 <tr> 用来定义表格中一行，<td> 用来定义表格中一个单元格等。
<div> 标签是 HTML 文档的分区元素，它定义了文档中一个逻辑上或视觉上的区域。它的开始标签和结束标签必须成对出现，并且可以放在 <body> 标签内部。它可以包含任何其他元素，并且可以通过 CSS 来设置其样式和布局。

补充：
 <a> 标签用于创建超链接，可以链接到其他网页或文档。
 <abbr> 标签用于表示缩写词，可以显示完整的单词或短语。
 <acronym> 标签用于表示取首字母的缩写词，可以显示完整的单词或短语。
 <address> 标签用于表示地址信息，通常以斜体显示。
 <alt> 属性用于为图片提供替用的文本，当图片无法显示时，可以提示用户图片的内容。
 <b> 标签用于将文本显示为粗体。
 <bdo> 标签用于改变文本的显示方向，可以实现从右到左或从左到右的效果。
 <big> 标签用于将文本显示为比正常大一号的字体。
 <blockquote> 标签用于表示区块引用语，通常以缩进的形式显示。
 <br> 标签用于在文本中插入换行。
 <cell>、<cellpadding>、<cellspacing> 这些标签或属性已经不推荐使用，它们用于定义表格中的单元格、单元格的补白和单元格之间的空间。
 <center> 标签用于将文本或其他元素居中显示。
 <cite> 标签用于表示引用的来源，通常以斜体显示。
 <code> 标签用于表示源代码或其他计算机相关的文本，通常以等宽字体显示。
 <dd> 标签用于表示定义列表中的定义描述，通常与 <dt> 标签配合使用。
 <del> 标签用于表示被删除的文本，通常以删除线显示。
 <dfn> 标签用于表示定义列表中的定义条目，通常以斜体显示。
 <div> 标签用于表示一个分隔区域，可以对其应用样式或脚本。
 <dl> 标签用于表示一个定义列表，包含一组术语和描述。
 <dt> 标签用于表示定义列表中的定义术语，通常与 <dd> 标签配合使用。
 <em> 标签用于表示强调的文本，通常以斜体显示。
 <font> 标签用于设置文本的字体、颜色和大小。
 <h1>~<h6> 这些标签用于表示标题，从 1 到 6 表示不同级别的标题，其中 1 是最高级别，6 是最低级别。
 <hr> 标签用于在文档中插入一条水平线，可以作为内容的分隔符。
 <href> 属性用于为超链接指定目标地址，通常与 <a> 标签配合使用。
 <i> 标签用于将文本显示为斜体。
 <iframe> 标签用于在网页中嵌入另一个网页或文档，可以创建内联框架。
 <ins> 标签用于表示被插入的文本，通常以下划线显示。
 <kbd> 标签用于表示键盘输入或其他用户输入的文本，通常以等宽字体显示。
 <li> 标签用于表示列表中的一个项目，通常与 <ul> 或 <ol> 标签配合使用。
 <nl> 标签用于表示导航列表，可以包含一组链接，通常与 <a> 标签配合使用。
 <ol> 标签用于表示一个有序列表，列表中的项目会按照数字或字母的顺序排列。
 <optgroup> 标签用于表示一个选项组，可以将一组相关的选项归类在一起，通常与 <select> 标签配合使用。
 <p> 标签用于表示一个段落，段落之间会有一定的间距。
 <pre> 标签用于表示预定义格式的文本，文本会保留空格和换行符，通常以等宽字体显示。
 <q> 标签用于表示引用语，引用语会被双引号包围。
 <rel> 属性用于为超链接指定关系类型，可以描述当前文档和链接文档之间的关系。
 <s> 或 <strike> 标签用于将文本显示为删除线。
 <samp> 标签用于表示示例或样本输出的文本，通常以等宽字体显示。
 <small> 标签用于将文本显示为比正常小一号的字体。
 <span> 标签用于表示一个范围，可以对其应用样式或脚本。
 <src> 属性用于为图片或媒体指定源文件的地址，通常与 <img> 或 <audio> 等标签配合使用。
 <strong> 标签用于将文本显示为加重的效果，通常以粗体显示。
 <sub> 标签用于将文本显示为下标，文本会稍微低于正常行高，并缩小字号。
 <sup> 标签用于将文本显示为上标，文本会稍微高于正常行高，并缩小字号。
 <td> 标签用于表示表格中的一个单元格，单元格可以包含文本或其他元素。
 <th> 标签用于表示表格中的一个表头单元格，表头单元格通常以粗体和居中的方式显示。
 <tr> 标签用于表示表格中的一行，一行可以包含多个单元格或表头单元格。
 <tt> 标签用于将文本显示为打印机或电传打字机的效果，通常以等宽字体显示。
 <u> 标签用于将文本显示为下划线。
 <ul> 标签用于表示一个无序列表，列表中的项目会以符号（如圆点）作为标记。
 <var> 标签用于表示变量或占位符的文本，通常以斜体显示。
